CREATE TABLE wfd_assignments (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    day_id INTEGER
        REFERENCES wfd_days(id) ON DELETE CASCADE NOT NULL,
    dish_id INTEGER
        REFERENCES wfd_dishes(id) ON DELETE CASCADE NOT NULL 
);